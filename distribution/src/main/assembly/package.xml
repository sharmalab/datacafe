<assembly>
	<id>package</id>
	<formats>
		<format>tar.gz</format>
		<format>zip</format>
	</formats>
	<dependencySets>
		<dependencySet>
			<outputDirectory>jars</outputDirectory>
			<includes>
				<include>${project.groupId}:datacafe-core:jar</include>
				<include>${project.groupId}:datacafe-core:jar:sources</include>
				<include>${project.groupId}:datacafe-server:jar</include>
				<include>${project.groupId}:datacafe-server:jar:sources</include>
				<include>${project.groupId}:datacafe-client:jar</include>
				<include>${project.groupId}:datacafe-client:jar:sources</include>
				<include>${project.groupId}:datacafe-samples:jar</include>
				<include>${project.groupId}:datacafe-samples:jar:sources</include>
				<include>${project.groupId}:datacafe-client-samples:jar</include>
				<include>${project.groupId}:datacafe-client-samples:jar:sources</include>
			</includes>
		</dependencySet>
		<dependencySet>
			<outputDirectory>sources</outputDirectory>
			<unpack>true</unpack>
			<unpackOptions>
				<excludes>
					<exclude>META-INF/</exclude>
				</excludes>
			</unpackOptions>
			<includes>
				<include>${project.groupId}:datacafe-core:jar:sources</include>
				<include>${project.groupId}:datacafe-server:jar:sources</include>
				<include>${project.groupId}:datacafe-client:jar:sources</include>
				<include>${project.groupId}:datacafe-samples:jar:sources</include>
				<include>${project.groupId}:datacafe-client-samples:jar:sources</include>
			</includes>
		</dependencySet>
		<dependencySet>
			<unpack>true</unpack>
			<unpackOptions>
				<excludes>
					<exclude>META-INF/</exclude>
				</excludes>
			</unpackOptions>			
			<outputDirectory>/</outputDirectory>
			<includes>
				<include>${project.groupId}:documentation</include>
			</includes>
		</dependencySet>
	</dependencySets>
</assembly>
